language: php
php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm
  - 7

before_install:
  - composer self-update

install:
  - composer install

script: vendor/bin/phpunit

matrix:
  allow_failures:
    - php: 7.0
  fast_finish: true

sudo: false

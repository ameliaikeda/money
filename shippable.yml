language: php
php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm
  - 7

before_install:
  - composer self-update

install:
  - composer install

script: vendor/bin/phpunit --log-junit shippable/testresults/phpunit.xml

matrix:
  allow_failures:
    - php: hhvm
    - php: 7
  fast_finish: true

sudo: false
